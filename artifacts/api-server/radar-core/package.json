{
  "name": "hookpoint-opportunity-radar",
  "version": "1.1.0",
  "private": true,
  "description": "Connector-ready warm-lead intelligence and activation system for Hook Point and Hyper Ads.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "NODE_ENV=development node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test --test-reporter=spec",
    "check": "bash -o pipefail -c 'for dir in src test; do [ -d \"$dir\" ] || { echo \"check: expected source directory \\\"$dir\\\" is missing\" >&2; exit 1; }; done; find src test -name \"*.js\" -type f -print0 | xargs -0 -n1 node --check'"
  },
  "license": "UNLICENSED"
}
